[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.2"
dependencies = ["pytest>=8.3.5"]

[tool.pytest.ini_options]
markers = ["task: marks tests as task tests"]

[tool.taskipy.tasks]
pytest = "uv run pytest"
